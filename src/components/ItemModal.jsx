import React, { useState } from 'react';

const ItemModal = ({ order }) => {
	const [ message, setMessage ] = useState('');

	const onSubmit = (e) => {
		e.preventDefault();
	};
	return (
		<>
        </>
	);
};

export default ItemModal;
